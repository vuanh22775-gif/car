# ── Server ─────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5000

# ── Database (MongoDB Atlas khuyến nghị khi deploy Vercel) ──────────────────
MONGODB_URI=mongodb+srv://vuanh22775_db_user:tuananh@cluster0.xxxx.mongodb.net/moto_db?retryWrites=true&w=majority# ── Auth ─────────────────────────────────────────────────────────────────────
JWT_SECRET=thay-doi-chuoi-bi-mat-nay
JWT_EXPIRE=7d

# ── Cloudinary (bắt buộc khi deploy Vercel — dùng để lưu ảnh xe) ────────────
# Nếu để trống 3 biến này, server sẽ tự lưu ảnh vào ổ đĩa cục bộ (chỉ dùng khi
# chạy "npm run dev" ở máy cá nhân; sẽ KHÔNG hoạt động trên Vercel).
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ── Email (nếu dùng nodemailer để gửi mail) ──────────────────────────────────
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASSWORD=
